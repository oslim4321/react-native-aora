import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Bookmark = () => {
  return (
    <View>
      <Text>thus is a Bookmark</Text>
    </View>
  );
};

export default Bookmark;

const styles = StyleSheet.create({});
